---
import { Image } from "astro:assets";
const { company, title, body, url, imgRef } = Astro.props;
---

<li class="card">
	<a href={url}>
		<div class="img-hover-zoom">
			<Image
				src={imgRef}
				alt="A variety of Virgin Atlantic Design System components and patterns, arranged into a collage"
				class="img"
			/>
		</div>
		<div class="content-wrapper">
			<div class="subtitle" aria-hidden="true">{company}</div>
			<h3>{title}</h3>
		</div>
	</a>
</li>

<!-- <li class="link-card">
	<a href={url}>
		<h3>
			{title}
		</h3>
		<p>
			{body}
		</p>
	</a>
</li> -->
<style>
	.card {
		border-radius: 0.5rem;
		/* box-shadow: 0.05rem 0.1rem 0.3rem -0.03rem rgba(0, 0, 0, 0.45); */
		padding-bottom: 1rem;
	}

	.card > :last-child {
		margin-bottom: 0;
	}

	.card h3 {
		margin-top: 1rem;
		font-size: 1.25rem;
	}

	.img-hover-zoom {
		height: inherit;
		overflow: hidden;
		border-radius: 0.5rem;
	}

	.img-hover-zoom img {
		transition: transform 0.5s ease;
	}

	a:hover img {
		transform: scale(1.1);
		border-radius: 0.5rem;
	}

	img {
		border-radius: 0.5rem;
		width: 100%;
		object-fit: cover;
		aspect-ratio: 16/9;
		max-height: max(10rem, 30vh);
	}

	img ~ * {
		margin-left: 1rem;
		margin-right: 1rem;
	}

	.content-wrapper {
		padding: 1rem;
	}

	/* .link-card {
		list-style: none;
		display: flex;
		background-color: #eeeeee;
		background-size: 400%;
		border-radius: 7px;
		background-position: 100%;
		transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
		box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.1);
		margin-bottom: 1rem;
	}
	.link-card > a {
		width: 100%;
		text-decoration: none;
		line-height: 1.4;
		padding: calc(1.5rem - 1px);
		border-radius: 8px;
		color: #181818;
		opacity: 0.8;
	}
	p {
		margin-top: 0.5rem;
		margin-bottom: 0;
	}
	.link-card:is(:hover, :focus-within) {
		background-position: 0;
		background-image: var(--accent-gradient);
	}
	.link-card:is(:hover, :focus-within) h2 {
		color: rgb(var(--accent-light));
	} */
</style>
